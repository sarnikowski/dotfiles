# Official-repo packages, one per line. Comments and blank lines are ignored.
# Installed on fresh machines by bin/bootstrap via `omarchy pkg add`.

fzf
ripgrep

# Upstream JetBrains Mono (all weights incl. Medium) alongside Omarchy's
# nerd-basic font, so foot can render text at medium weight and fall back
# to the Nerd Font for icon glyphs.
ttf-jetbrains-mono
